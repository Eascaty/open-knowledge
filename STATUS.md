# 项目状态

- 更新时间：2026-09-26（Asia/Shanghai）
- 当前阶段：第一、第二阶段已完成；`0.5.0` 已正式发布，继续优化日常阅读与投放体验。
- 总体状态：知识库可在本机网页直接投放资料；长 Markdown 可拆成独立知识卡；队列、候选站发布、备份与恢复演练具备明确完成语义。Docker 仅是可选 Java 只读 API 部署层，不参与日常知识整理。

## 当前能力

- 空的本机私密库提供首次使用引导：可预览虚构示例或导入自己的资料；示例须明确提交，不覆盖未提交草稿。整理完成后提供知识卡入口和搜索提示；公开站不显示此引导，无本机写入能力时只显示阅读说明。

- 新增完整本地产品包构建：明确白名单收集 Python 程序、Web 资源、契约、命令和虚构示例，输出可重复构建的 tar.gz、逐文件清单与 SHA-256。解压后不依赖开发仓库/Git/Java；仍需 Python 3.9+，支持范围为 macOS/Linux，尚未正式 Release。

- 严格的专业母子分类树，每张知识卡只有一个主路径；一份原始来源可拆成多张卡，跨领域关联使用辅助关系。
- 本地网页支持拖放/多选、直接粘贴文字或 Markdown、两路并发、逐条进度、刷新恢复、重复识别和完成后打开知识。
- 阅读知识卡时自动推荐最多5条相关知识，并明确标注“同一原文”“共同标签”“同一专业”或“相邻专业方向”；推荐不改变主分类树，也不写回数据库。
- 本机知识详情支持人工调整主分类：选择目标后先预览再确认，完成后安全重建网站，并提供15分钟内的撤销入口；活动节点校验、乐观并发、搜索路径和审计事件仍由唯一原子服务负责。
- 本机知识详情支持人工维护可信状态：待验证、个人认知、有证据支持、实践验证、存在争议和已过时均可先预览再确认；状态通过追加审计事件保存，不改写原文、来源或主分类。
- 本机顶部提供可信审核队列：汇总各状态数量，优先展示存在争议、已过时和待验证的知识，可按六种状态筛选并直接进入下一张知识卡；公开站不显示入口。
- 知识详情支持安全的 Markdown 阅读器：标题、列表、引用、表格、代码块、链接和强调语法会排版显示，长文提供目录跳转，并支持下载当前卡片正文。
- 可信审核支持附加说明和私密追加历史；说明不会进入公开构建或 Java 只读 API。
- 知识详情增加本机阅读操作：可复制当前卡片链接或正文，并在浏览器本地保留最多8条最近阅读记录；记录只保存知识卡 ID 和时间，不进入 SQLite、静态站或公开构建。
- 搜索支持专业路径（含下级目录）、标签、可信状态与“全部 / 知识卡 / 专业节点”组合筛选；多个空格分隔关键词分别匹配，可无关键词浏览筛选结果，清除筛选会保留查询；关系地图同时提供可键盘访问的节点列表兜底。
- ChatGPT/Gemini 官方导出支持本地 JSON、HTML、ZIP 和解压目录导入；导入文件先生成稳定 Markdown，再复用现有收件箱、SHA-256 去重、拆卡、分类和网站发布链路。
- Ollama 作为可选本地提炼器时增加严格 JSON、响应大小和关系置信度校验；本地模型不可用或响应不合规时自动回退到规则引擎，并把 `rules-fallback-v1` 写入知识元数据。
- 已增加本机站点分享包：通过构建门禁后，将私密或公开静态站点打成带清单和 SHA-256 的 ZIP；包只含站点产物，不自动上传，也不包含数据库和原始资料。
- 分享包支持离线验包：校验整体 SHA-256、路径穿越、符号链接、清单总数和每个文件摘要，且不解压、不修改项目文件。
- 已增加完整私密项目备份包：把 SQLite 一致性快照、收件箱、raw/normalized/quarantine、Vault、站点产物和配置打成清单化 ZIP；日志、控制状态、WAL/SHM 不进入包。候选 ZIP 自动通过逐文件摘要与数据库验证后才发布为最终备份；失败清理候选并保留旧备份，不覆盖正式库。新增 `scripts/restore-backup-bundle`，在全新目录恢复 schema v2 备份；核验文件引用与原件摘要，保留数据库、人工分类和审核，离线重建私密网站并通过 Gate/Health 后写入完成标记。恢复数据目录仍需当前源码运行。
- 已增加 `scripts/publish-plan`：只检查 Cloudflare Pages 发布计划和本地门禁，不登录、不联网、不上传；真实私密发布仍需单独确认 Access。
- 浏览器写入控制面只绑定 `127.0.0.1`，使用独立内存会话、Host/Origin 校验、大小/类型限制、私密暂存和原子 inbox 发布；公开静态站保持只读。
- Python 流水线完成 SHA-256 去重、不可变 raw、解析、提炼、分类、Vault、FTS5、关系图和静态站构建，默认零网络、零付费 API。
- SQLite schema v2 支持一个 source 对应多张 document 知识卡；长 Markdown 按 H2 确定性拆卡，并保存标题路径、原文行号、正文哈希和拆分器版本。
- 未拆分资料与首卡保留原 source ID，新增卡使用稳定派生 ID；分类、关系、FTS、Vault 与 canonical 均以知识卡为粒度。
- 旧 v1 数据库只通过 `scripts/migrate` 显式升级：迁移前自动一致性快照，事务内验证完整性和外键，失败完整回滚；本机真实私密数据库已于 2026-08-30 按运行手册迁移到 v2 并通过恢复演练。
- `scripts/restore-drill` 在临时候选库中恢复并核验快照、SHA-256、schema、关键表与行数，绝不覆盖正式数据库。
- `scripts/acceptance` 只使用固定虚构资料验证嵌套输入、重复、长文拆卡、队列续跑、失败隔离、发布门禁和零网络。
- 流水线以数据库实际 `queued/retry/running` 数为完成依据；待处理任务、Gate 失败或 Health 失败均不替换上一版站点。
- 知识管家启动和空闲轮询会检查数据库待办，迁移重排或修复任务无需再次改动收件箱即可自动续跑；历史上发生过重试但最终已排空的任务可以正常发布。
- 知识管家启动采用可回滚状态切换：新后台未通过存活验证时会终止失败子进程并恢复上一份控制状态，避免一次启动失败覆盖仍有价值的实例信息。
- 单条终态失败被隔离；队列排空且候选检查通过后，同批正常知识仍可发布，知识管家显示 `degraded` 并停止空转。
- Java 21 API 和受限离线导入兼容 SQLite schema v1/v2；HTTP 仍只读 public 内容，canonical JSON 与 OpenAPI 继续保持独立的 v1 契约。
- Release 工作流使用 artifact v7/v8，并对固定公开契约执行上传、下载和 SHA-256 往返校验。

- 本机知识卡支持按来源行号展开文本、Markdown、代码以及 DOCX/HTML 提取文本；先核验 SHA-256，限量只读显示。DOCX/HTML 行号属于提取文本，不代表原文件页码；PDF 与旧版 DOC 暂不支持原件预览，公开站和离线分享包不提供此入口。

- 搜索结果显示最长180字符的匹配上下文并突出标题、片段中的关键词；优先使用已加载正文，无需先打开卡片；显示内容通过纯文本 DOM 创建。

- 从搜索打开知识卡后可返回原搜索，恢复关键词、全部筛选、列表滚动位置和所选结果焦点；查询只保留在当前页面内存，刷新清除。

- 搜索支持每页30条并前后翻页；改变关键词或筛选回到首页，从卡片返回搜索保留原页。

- 搜索支持方向键选结果、回车打开与 Tab 焦点循环；中文输入法组合期间不触发快捷操作，筛选重绘后保留焦点。

- 已用65条虚构资料完成桌面及390×844窄屏搜索验收；修复结果滚动带走输入框/关闭按钮的问题，补齐手机搜索图标名称。

- 从复核队列打开卡片后可返回原队列，恢复状态筛选、展开数量、滚动位置及焦点；刷新或审核保存后的页面重载清除本次上下文。

- 需复核卡片可“本次暂缓”，从需处理列表移出并在暂缓列表中恢复；总待复核数与可信状态不变，刷新后恢复。

- 复核队列支持标题、摘要、标签与专业路径的多关键词筛选，与状态和本次暂缓组合使用；阅读返回恢复关键词，清除关键词保留状态范围。

- 新增1,000/10,000张虚构卡片的搜索与复核逻辑基准，10种场景自动校验并在 CI 留存测量报告；搜索提前排除不匹配卡片，减少无效字段处理。详见[基准报告](docs/web-benchmark.md)。

- 新增千卡/万卡静态建站基准：临时目录验证私密/公开产物、搜索完整性、私密标记排除及重复构建一致；CI 保留报告。构建基准不替代完整发布门禁，详见[建站报告](docs/site-benchmark.md)。

- 新增桌面 Chrome 千卡/万卡真实页面基准：独立临时配置验证页面就绪、搜索首屏/唯一命中/空结果，并记录 JS 堆快照；本机万卡页面约383毫秒就绪。仅为本机回环网络的无界面桌面结果，真实手机和进程峰值内存未测，详见[浏览器报告](docs/browser-benchmark.md)。

## 最近验证

- Python：2026-09-18 在允许本机回环端口的环境完整复验 135/135 通过，包含知识管家 HTTP/进程测试、备份候选损坏隔离、新目录恢复的7项场景与本地产品包的3项验收；全部使用临时虚构资料。
- Web：90/90 通过（静态/API 数据源 2 项，搜索规则 31 项，搜索交互 14 项，相关知识 2 项，网页投放 10 项，本机归类与撤销 4 项，可信审核 4 项，可信审核队列 7 项，Markdown 阅读器 4 项，阅读历史 4 项，阅读操作 2 项，首次使用 4 项，原文读取 2 项）。
- 批量虚构验收：5 个输入、4 个唯一来源、6 张知识卡、3 张拆分卡；重复运行无新增，1 个坏资料隔离且正常资料保留，Gate/Health 通过，网络请求 0。
- Java：合并后本机 38/38 通过，JaCoCo 指令 89.5%、分支 76.5%；远程 CI 将再次执行 Java 21 与容器门禁。
- `git diff --check`、canonical 契约增量字段、私密路径排除和固定公开 Demo 边界持续由自动测试保护。

## 使用与升级

- 日常入口：双击 `打开知识库.command`，或运行 `scripts/knowledge-manager start`。
- 新安装直接创建 SQLite schema v2。
- 已有 v1 私密数据库先停止知识管家，再人工运行 `scripts/migrate`；真实迁移和恢复必须按 `docs/runbooks/backup-restore.md` 操作并记录运维日志。
- 原始资料、SQLite、Vault、私密网站、缓存和隔离文件继续位于 Git 忽略的 `workspace/`，只有 `exports/public/` 可作为公开候选。

## 后续里程碑

当前优先执行[本地产品路线](docs/product-roadmap.md)：收口分支与备份自验（L01/L02），L03–L05 已在功能分支实现，L06/L07/L10 已交付当前子项，L15 已建立逻辑基准，待 PR 检查与合并。完整优先级和商业化闸门见[分析方案](docs/product-strategy-2026-09-08.md)。以下远程与原生方向保留为有条件事项。

1. 用户准备上线真实私密站点时，再配置 Cloudflare Pages + Access；默认不上传 private 数据。
2. 确认 Java API 的真实远程访问者后，再设计认证、TLS 和同步；容器化本身不等于公网安全。
3. 确认原生 App 的目标平台、离线编辑和同步冲突策略后，再创建移动端工程；客户端复用 OpenAPI，不直连 SQLite。
