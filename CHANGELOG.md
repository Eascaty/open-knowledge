# 版本记录

## Unreleased

- 增加 ChatGPT/Gemini 官方导出本地导入：支持 JSON、HTML、ZIP 和解压目录，解析后生成稳定 Markdown，复用既有去重、拆卡、分类、审核与网站链路；不连接账号、不发起网络请求。
- 加固可选 Ollama 提炼器：限制响应大小，严格校验摘要、标签、路径和关系结构；本地模型不可用或输出不合规时自动回退到规则引擎。
- 增加本机 `scripts/package-site`：校验站点构建清单后生成带文件清单和 SHA-256 的可分享 ZIP，不联网、不上传、不打包原始资料。
- 增加 `scripts/verify-site-package`：上传前后离线验证分享包整体哈希、清单和逐文件完整性，不解压。
- 可信审核支持填写审核说明，并在私密知识卡中显示最近说明和追加式审核历史；公开构建不会携带个人审核笔记。
- 知识详情增加安全 Markdown 阅读器：支持标题目录、列表、引用、表格、代码块、受限外链与当前知识卡正文下载。
- 知识详情增加本机阅读操作：支持复制卡片链接、复制正文和最多8条最近阅读记录；浏览器只保存卡片 ID/时间，不写回知识库。
- 搜索窗口增加知识卡/专业节点范围筛选；关系地图增加列表视图，触屏、键盘和画布不可用时仍可打开节点。
- 网页投放中心支持直接粘贴纯文字或 Markdown，并复用原有私密暂存、原子入箱、进度恢复和去重链路。
- 知识详情增加最多5条可解释的相关知识导航，按同一原文、共同标签、同一专业和相邻方向确定性排序；不写回事实关系，不调用网络或模型。
- 知识详情增加本机人工归类：选择目标后先预览、确认再保存，并提供限时撤销入口；复用项目锁、乐观并发、原子索引/审计和候选站发布，公开站继续只读。
- 知识详情增加本机可信状态审核：支持待验证、个人认知、有证据支持、实践验证、存在争议和已过时，状态变化追加审计事件并自动更新网站。
- 本机页面增加可信审核队列：汇总状态数量，优先排列争议、过时和待验证知识，支持按状态筛选并直接进入知识卡；公开站保持隐藏。

## 0.5.0 — 2026-08-31

- 升级 GitHub Actions artifact 上传运行时，并在受保护的 Public Demo 检查中用固定公开契约完成上传、下载与 SHA-256 往返验证。
- 增加纯 Python 本地知识管家：单实例后台监听收件箱、自动运行全流水线并持续提供知识网站。
- 增加 macOS `打开知识库.command` 双击入口、首次构建等待页、状态/停止命令和失败保留旧站策略。
- 本地控制仅绑定回环地址，控制令牌和完整错误保存在 Git 忽略的私密工作区；网页状态使用脱敏字段白名单，静态服务拒绝符号链接越界读取。
- 增加网站内“投放资料”中心：拖放/多选文件、上传进度、刷新后状态恢复、自动重试提示，并在完成后打开对应知识；公开静态 Demo 自动保持只读。
- 增加独立浏览器会话、Host/Origin 防护、64 MiB 上限、两路并发、未监控私密暂存与原子 inbox 发布；Service Worker 不缓存管理接口和构建版本。
- 网站缓存版本同时跟随知识数据和前端资源变化；DOCX 提取限制解压大小与压缩比，拒绝压缩炸弹。
- SQLite 升级为 schema v2：一份不可变来源可以生成多张独立知识卡；长 Markdown 按二级标题确定性拆分，并保留标题路径、原文行号、正文哈希和拆分器版本。
- 增加显式 v1→v2 迁移：迁移前自动创建一致性快照，事务内校验完整性与外键，旧 Markdown 来源重新排队；不再隐式修改已有数据库。
- 增加批量虚构验收与非破坏性恢复演练入口，覆盖嵌套目录、重复输入、队列续跑、单条失败隔离、快照哈希和损坏快照拒绝。
- 流水线成功语义改为以数据库实际待处理任务为准；候选站只有在队列排空且 Gate/Health 通过后才原子晋升，终态坏资料不阻塞同批正常知识。
- 知识管家启动时同时检查数据库待办，即使迁移或修复没有改变收件箱文件也会自动续跑；本轮曾重试但最终队列排空时可正常发布候选站，不再出现假成功或重复空转。
- 公开仓库迁移到 `Eascaty/open-knowledge`，同步修正项目元数据、演示地址和安全报告入口；既有运行时服务名、容器名和 v1 契约标识保持兼容。

## 0.4.0 — 2026-08-23

- 将 Python、Java、Web 重整为 `apps/`，共享契约集中到 `packages/contracts/`，真实私密运行状态统一到 Git 忽略的 `workspace/`。
- 按存储、处理、运维检查和网站构建职责拆分 Python 大模块，并保留旧导入兼容 facade。
- 增加 canonical JSON Schema v1、OpenAPI v1、标准库契约校验和 Python/Java 共用虚构样例。
- Web 增加静态包与 API v1 数据源适配器；Java 增加控制器与只读仓储之间的应用服务层。
- CI 增加 Web 适配器测试；v0.4.0 tag 覆盖 Python 30项、Java 34项、Web 2场景、Demo、健康门禁和桌面/手机浏览器验收。
- 精简分支与文档治理规则，本机运行配置不再进入 Git，保留可复制的 `runtime.example.json`。
- Java 搜索升级为 SQLite FTS5 trigram、BM25 排序、安全高亮和参数化 LIKE 降级，并增加固定中文数据性能基准。
- 增加受限 Java 离线文件导入 CLI、跨 Python/Java 的 POSIX 项目锁以及 raw/SQLite 回滚和幂等测试；HTTP API 保持只读。
- Python 包、Java JAR 与运行时版本统一为 `0.4.0`，自动测试阻止跨模块版本再次漂移。
- 增加非 root、只读文件系统的 Java API 容器、数据库就绪探针和真实 Linux 容器门禁；Docker 保持可选部署层。
- 增加 tag/多运行时版本一致性校验、版本化 JAR、SHA-256 与 GitHub build provenance 的可复现 Release 流水线。

## 0.3.1 — 2026-08-09

- 增加带 SHA-256 校验的 Maven Wrapper，不再要求预装 Maven。
- 增加 `.gitattributes` 与 CODEOWNERS，固定跨平台脚本行尾和代码所有权。
- CI 升级 Actions 与 JaCoCo，增加并发取消、超时和覆盖率硬门槛。
- Dependabot 保留 Spring Boot 3 主线，不再自动提交 4.x 跨大版本升级。
- Python 包与 Java JAR 统一为正式 `0.3.1` 版本。
- 启用 main 分支保护、Secret Scanning、Push Protection、Dependabot 安全更新、私密漏洞报告与 CodeQL。

## 0.3.0 — 2026-08-09

- 增加 Java 21 + Spring Boot 3.5 的只读知识服务。
- 增加健康检查、分类树、知识列表、详情与基础搜索 API v1。
- Java API 仅返回公开知识，并对来源绝对路径进行结构性裁剪。
- 增加 7 项 JUnit、SQLite 集成、MockMvc 和 canonical JSON 契约测试。
- GitHub Actions 增加 Java 21 Maven 构建与 JaCoCo 覆盖率报告。

## 0.2.0 — 2026-08-09

- 使用 Apache License 2.0 开源。
- 增加贡献指南、安全策略、Issue/PR 模板、Dependabot 和 GitHub Actions CI。
- 明确 Python MVP 与 Java 21/Spring Boot 增量演进路线。
- 公开仓库仍严格排除真实知识、数据库、私密导出和生成站点。
- 完成本地文件到 SQLite、Markdown、全文搜索、知识图和静态网站的闭环。
- 新增 `scripts/run-pipeline` 单命令自动化和项目锁。
- 新增 private/public 构建、PWA、安全响应头与 Cloudflare 发布适配。
- 新增数据库、磁盘、隐私、断链、构建清单和一致性备份检查。
- 修复 public 发布门禁、分类节点退休、symlink、raw 路径越界和并发竞态。
- 18 项自动测试与三资料端到端样例全部通过。

## 0.1.0 — 2026-07-27

- 建立 `$HOME/ai/knowledge` 单一工程根目录。
- 固化金融、AI、技术三条专业母子链路与 Java 子树。
- 采用 Python 标准库、SQLite、Markdown/JSON 和纯静态网站的零依赖核心方案。
- 默认本地运行、默认私密、默认不连接外网和不执行真实发布。
- 开始实现入库、去重、分类、搜索、知识树、知识图、网站、健康检查、备份和发布门禁。
